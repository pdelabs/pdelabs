import { Title } from '@/components/Typography/Typography';
import React from 'react';

const AboutUs = () => {
    return (
        <section id="about-us" style={{ paddingTop: '5rem' }}>
            <Title>About us</Title>
            <div style={{ height: '100vh' }} />
        </section>
    );
}

export default AboutUs; 